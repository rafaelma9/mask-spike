import { MaskingDirective } from './masking.directive';

describe('CpfDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskingDirective();
    expect(directive).toBeTruthy();
  });
});
